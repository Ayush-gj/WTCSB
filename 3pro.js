//user input in js via browser or via terminal

const name=prompt("Enter your name");
const age=prompt("Enter your age");
const info=`My name is ${name} and my age is ${age}`;
console.log(info);
document.write(info);